export const AUTH_CONFIG = {
    // Session duration in minutes
    SESSION_DURATION: process.env.NEXT_PUBLIC_SESSION_DURATION || 20,
    // SESSION_DURATION: 1,
    // Add other auth-related configuration here
  }